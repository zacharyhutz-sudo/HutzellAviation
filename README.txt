Hutzell Aviation favicon update

Drag the 'public' folder into the root of the GitHub repository and allow GitHub to replace matching files.

The existing site already references /favicon.png, so replacing public/favicon.png is sufficient for the current site. The additional ICO/PNG sizes are included for browser and device compatibility.
